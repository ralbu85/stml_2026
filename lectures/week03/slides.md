---
marp: true
theme: default
paginate: true
size: 16:9
math: katex
header: '머신러닝 특론 — LLM 에이전트 (2026)'
footer: 'Week 03 · 추론 모델 (Reasoning / Test-time Compute)'
style: |
  section { font-size: 26px; }
  section h1 { font-size: 42px; }
  section h2 { font-size: 34px; }
  table { font-size: 22px; }
  code { font-size: 21px; }
  .small { font-size: 19px; color: #666; }
---

<!-- _class: lead -->

# Week 03. 추론 모델

## 생각을 더 시키면, 얼마나 좋아질까?

머신러닝 특론 — LLM 에이전트 · 2026

<!--
[강의 전 준비] 지난주 만든 루프에 애매한 문제를 3번 던져 답이 흔들리는 화면(temperature 0.8).
오늘의 목표: '생각을 늘리는' 두 갈래(추론할 때 더 / 학습으로 내재화)를 하나의 지도로 세운다.
RL은 사전 지식 없음을 가정 — 중간에 개념만 3분 입문(수식 0).
-->

---

# 오늘의 지도 (이론 35분)

| 분 | 무엇을 | 남길 개념·키워드 |
|---|---|---|
| 0–5 | 문제 제기 | 출력이 실행마다 다른 이유 (확률적 표집) |
| 5–15 | 갈래 ① 추론할 때 더 | **test-time compute** · self-consistency |
| 15–29 | 갈래 ② 학습으로 내재화 | STaR → (RL 3분) → **DeepSeek-R1** |
| 29–35 | 설계와 연결 | 어떤 모델을 언제 · 발표·실습 |

**큰 그림:** 생각을 늘리는 두 갈래 — **① 추론할 때 더** vs **② 학습으로 몸에 배게.**

---

<!-- _class: lead -->

# 0–5′ · 문제 제기

---

# 출력의 비결정성 (같은 질문, 다른 답)

LLM의 출력은 **결정적이지 않다.** 같은 질문에도 실행마다 다른 답이 나올 수 있다.

- LLM은 확률 분포에서 토큰을 **표집(sampling)** 한다 — 매 실행이 서로 다른 추론 경로를 따른다
- 따라서 한 번의 응답은 여러 가능한 경로 중 **하나를 뽑은 표본**에 불과하다
- CoT는 프롬프트만으로 정확도를 크게 높였다. 이를 확장하는 두 방향이 오늘의 주제다:

**① 추론 시점에 계산을 더 쓰는 방법** · **② 잘 생각하는 능력을 학습으로 내재화하는 방법.**

<!--
[0–5분] 라이브로 3번 호출해 흔들리는 걸 보여준다. "오답을 뽑았다는 건, 다시 뽑으면 정답이 나올 수도 있다는 뜻" → 첫 갈래로.
-->

---

<!-- _class: lead -->

# 갈래 ① · 추론 시점의 계산 확대

---

# Self-Consistency — 다중 샘플 다수결

**Self-Consistency** = 같은 질문을 여러 번 샘플링해 나온 **최종 답들의 다수결**을 취하는 기법 (Wang et al., 2022).

구하려는 것은 답 $a$의 확률이다. 답은 중간 추론 경로 $r$를 거쳐 나오므로, 특정 경로 하나가 아니라 **가능한 모든 경로를 합산해야** 답의 실제 확률이 된다:

$$P(a \mid q) \;=\; \sum_{r} P(a \mid r, q)\,P(r \mid q)$$

- **한 번만 물으면**: 확률이 가장 높은 경로 하나가 내놓은 답만 본다 → 그 경로가 틀리면 그대로 오답
- **여러 번 샘플해 다수결**: 위 합을 **샘플로 근사**한 것 — 서로 다른 경로가 같은 답에 모이면 그 답이 이긴다

**유리한 이유:** 정답은 서로 다른 경로가 같은 결론으로 수렴하는 반면, 오답은 경로마다 제각각이어서 표가 분산된다. 따라서 합산(다수결)이 정답을 부각시킨다.

<p class="small">지난주 교수 시연이 바로 이 논문 — 향상 폭 수치는 그 시연·발표에서 확인</p>

<!--
[8분] 학생 문답: "이게 안 통하는 문제는?" → 다음 슬라이드로.
-->

---

# 일반화 — test-time compute

**test-time compute** = 학습(가중치)은 그대로 두고, **추론 시점에 계산을 더 써서** 성능을 올리는 접근. self-consistency는 그 한 예일 뿐 — 방법군 전체는:

| 방법 | 계산을 더 쓰는 방식 |
|---|---|
| Self-consistency | 병렬로 N번 → 다수결 |
| Best-of-N | N번 생성 → **검증기(verifier)** 가 최고 선택 |
| 더 긴 사고 | 한 경로를 깊게 (오늘 뒤의 o1·R1) |
| 트리 탐색 | 분기·평가·백트래킹 (→ W5) |

**공통 원리:** 학습은 그대로, 추론에서 계산 ↑ = 정확도 ↑ — 이를 **추론 시점 스케일링(inference-time scaling)** 이라 하며 o1·R1의 근간이다. (다수결과 달리 Best-of-N·트리 탐색은 답을 고를 검증기/보상 모델을 요구한다.)

**다만 이 계산은 공짜가 아니다** — N회 반복은 비용·지연도 N배다. 따라서 실무의 질문은 "N을 몇으로 둘 것인가"가 아니라 **"어떤 질문에만 N>1을 적용할 것인가"** 이다 (→ W8 적응형).

<!--
[13분] 여기서 '비용 축'을 심는다 — W13 평가(비용-정확도)의 씨앗. "정확도만 보고하는 건 반쪽"이라는 태도.
-->

---

<!-- _class: lead -->

# 갈래 ② · 추론 능력의 학습

---

# 선별에서 학습으로의 전환

다수결(갈래 ①)은 질문마다 N번의 추론을 반복하므로 비용이 크다. 여기서 핵심 관찰은 다음과 같다:

> 다수결로 **정답을 가려낼 수 있다면**, 그 정답에 이른 **좋은 풀이도 가려낼 수 있다.**

가려낸 좋은 풀이로 모델을 다시 학습시키면, 추론 시점에만 쓰던 계산이 **모델의 가중치 안으로 내재화**된다. 이 발상을 처음 구현한 것이 **STaR**(2022), 정식 RL로 대규모로 실현한 것이 **DeepSeek-R1**(2025)이다.

<!--
[15분] 오늘의 경첩. test-time(임시)과 training(영구)을 잇는 한 문장.
-->

---

# STaR — 자기 생성 풀이 부트스트랩

**STaR (Self-Taught Reasoner)** = 모델이 스스로 만든 풀이 중 **정답을 맞힌 것만 걸러 재학습**하는 부트스트랩 방법 (Zelikman et al., 2022).

```
① 풀이 생성: 문제마다 CoT로 풀게 한다
② 필터:     최종 답이 맞은 풀이만 남긴다
③ 재학습:   남은 풀이로 모델을 파인튜닝
④ 반복:     더 똑똑해진 모델로 ①부터 다시
```

- 사람이 풀이를 제공하지 않는다 — **소수의 예시만 시드로** 준다
- 핵심 착상: 풀이의 품질은 채점하기 어려워도 **답의 정오는 판정할 수 있다** — 이 정오가 필터가 된다
- 형식적으로는 **기댓값 최대화(EM)** 의 근사 — 맞는 풀이(잠재변수)를 추정하고(E), 그 풀이로 모델을 갱신(M)한다
- 지난주 배운 **자기 생성 + 유용성 필터** 패턴 — 도구(W4)에서 또 나온다

<p class="small">작은 모델이 훨씬 큰 모델을 따라잡은 수치는 오늘 발표 ①(STaR)에서 확인</p>

<!--
[20분] "6B가 스스로 가르쳐 175B급을 따라잡았다"는 임팩트는 발표자에게 넘긴다. 여기선 아이디어의 논리만.
-->

---

# 강화학습(RL) — 정의와 직관

**강화학습(RL)** = 정답을 시연하지 않고 **보상(점수)만으로** 정책을 개선하는 학습. 학습 대상인 행동 규칙을 **정책(policy)** 이라 하고, LLM에서는 **모델 자체가 정책**이다.

비유 — 강아지: 정답 동작을 보여줄 수 없으니, 우연히 앉으면 간식을 준다. 간식 받은 행동이 잦아진다.

| | 지도학습 (SFT) | 강화학습 (RL) |
|---|---|---|
| 주는 신호 | **정답 시연** | **보상(점수)** |
| 배우는 것 | 시연을 흉내 | 보상을 높이는 행동 |
| 한계·잠재력 | 시연자를 넘지 못함 | 시연 없이 **시연자를 넘을 수도** |

<!--
[21분] "RL 처음 듣는 분 기준"으로 시작. 마지막 줄(시연 없이 사람을 넘음)이 R1의 복선.
-->

---

# LLM에서의 RL — RLHF와 RLVR

LLM에 대응시키면: **행동** = 답안 하나를 생성 · **보상** = 그 답안의 점수 · **학습** = 보상을 높이는 방향으로 정책(모델)을 갱신.

보상을 **누가 매기느냐**로 두 갈래 — 이 구분이 R1을 이해하는 열쇠다:

- **RLHF** (Reinforcement Learning from Human Feedback) — 사람 선호로 학습한 **보상 모델**이 채점. ChatGPT 정렬의 방법.
- **RLVR** (RL with Verifiable Rewards) — **규칙으로 검증 가능한 보상**: 정답 일치·테스트 통과·형식 준수. 학습된 보상 모델이 없어 **조작(해킹)이 어렵다.** → 오늘의 R1.

> 정책을 실제로 갱신하는 알고리즘(PPO·GRPO 등)은 이 과목 밖 — 발표의 선택 심화로.

<!--
[23분] RLHF/RLVR 구분을 반드시 각인 — R1이 RLVR이기 때문에 검증 가능한 도메인(수학·코드)에서만 통한다는 한계로 이어진다.
"보상 모델 vs 규칙 보상"의 차이가 R1의 단순함과 보상 해킹 저항성을 설명한다.
-->

---

# DeepSeek-R1 — 순수 RL과 추론 창발

**DeepSeek-R1** = 사람의 풀이 시연 없이 **순수 RL만으로 추론 능력을 학습**시킨 공개 모델 (2025).

STaR의 필터("정답인 풀이만 남긴다")는 사실상 **보상**이다. 이를 정식 RL로, 더 큰 모델에서 충분히 오래 수행한 것이 R1이다.

**R1-Zero의 레시피 (RLVR의 극단):**
- 베이스 모델에서 **곧바로 RL** — SFT 콜드스타트도, 사람 시연도 없음
- 보상은 **규칙 기반 두 가지**(정답 일치 · 형식 준수) — 학습된 보상 모델이 없어 **보상 해킹의 여지가 좁다**
- 갱신은 **GRPO** — 한 문제에 여러 답을 생성해 그룹 내 상대 우열로 학습한다(별도 가치함수 없이). 상세는 발표로

**창발(emergence)** = 규모나 학습이 일정 수준을 넘을 때, 작은 규모에선 없던 능력이 갑자기 나타나는 현상. R1에서는 **아무도 가르치지 않은 자기 교정이 창발했다** — 응답이 점점 길어지고, 검산·대안 탐색이 나타나며, 훈련 중 모델이 스스로 *"Wait, wait. 다시 보자"* 라고 쓴다.

<p class="small">AIME 창발 곡선·"aha moment" 원문은 오늘 발표 ②(R1)에서 수치·인용으로 확인</p>

<!--
[27분] 오늘의 정서적 피크. 균형: "단, 검증 가능한 보상(수학·코드)이 있는 도메인 이야기. 에세이엔 그대로 안 통한다" — 발표 토론거리.
-->

---

# 보상 설계 = 사양서 (두 접근의 결합)

R1-Zero의 생각은 **읽기 어렵고 언어가 섞였다** — 보상에 '가독성'이 없었으니까. R1은 소량의 좋은 예시로 다듬은 뒤(SFT) 다시 RL로 마감했다.

교훈 두 줄, 학기 내내 이어진다:
- **능력은 RL이 만들고, 태도는 SFT가 다듬는다**
- **보상에 넣지 않은 것은 얻지 못한다** — 보상 설계가 곧 사양서. 이것이 **보상 해킹(reward hacking)** 의 씨앗이다: 지표를 최적화하면 지표가 목표를 대변하기를 멈춘다(**Goodhart의 법칙**). W13에서 정면으로 다룬다

그리고 R1도 마지막엔 **다수결(갈래 ①)** 을 얹어 더 오른다 — **두 갈래는 곱해진다.**

<!--
[29분] "보상 설계 = 사양서"는 W13 평가·보상 해킹의 씨앗. 두 갈래가 대립이 아니라 합쳐진다는 점을 각인.
-->

---

<!-- _class: lead -->

# 설계와 연결

---

# 모델 선택 — 일반 모델 vs 추론 모델

추론 모델은 "생각을 얼마나 할지"를 **파라미터로 조절**한다 — 벤더마다 이름은 달라도 개념은 하나: **thinking budget(생각 예산).**

판단 기준:

| | 일반 모델 | 추론 모델 |
|---|---|---|
| 강점 | 도구 호출·정형 작업 | 수학·계획·까다로운 판단 |
| 루프 설계 | 여러 바퀴 돌며 보정 | 적은 바퀴, 한 수를 깊게 |

- **기본값은 저비용 모델** — 추론 모델은 병목 지점에만 투입한다
- '측정 없이 좋아 보인다'는 판단은 배제한다 — **근거는 측정**이다 (→ W13)

<!--
[32분] 벤더별 API 표는 안 띄운다 — "생각량은 조절 가능한 자원(thinking budget)"이라는 개념만 남긴다.
우리 앱 기준: planner(W5)엔 추론 모델이 어울리고, 요약엔 과하다.
-->

---

# 오늘의 연결 — 발표와 실습

**발표 ① STaR** — 스스로 만든 풀이로 부트스트랩하는 아이디어와, 작은 모델이 큰 모델을 따라잡는 수치.
**발표 ② DeepSeek-R1** — 순수 RL로 추론이 창발하는 곡선과 'aha moment'.

**오후 실습:** 갈래 ①을 직접 만든다 — 같은 질문을 N번 샘플링해 **다수결**(self-consistency). N=1과 N=5의 정확도를 **비용과 함께** 비교한다 — 계산을 더 쓴 만큼 값어치를 했는지까지가 실습이다.

<!--
[34분] 실습을 코드로 설명하지 않는다. "다수결을 만들고, 비용까지 잰다"는 개념으로만.
-->

---

# 요약

1. **Test-time compute** — 학습은 그대로, 추론에서 계산을 더 써서 정확도를 산다(다수결: 정답은 모이고 오답은 흩어진다)
2. **STaR** — 답의 정오로 좋은 풀이를 걸러 재학습 = 스스로 가르치기
3. **R1** — 그 필터를 제대로 된 RL로: 정답 보상만으로 긴 사고·자기교정이 **창발**한다

**다음 주 (W4. 도구 사용):**
지금까지 모델은 *생각*만 잘한다 — 계산기 하나 못 쓴다. **말을 행동으로 바꾸는 연결**을 개념부터 뜯고, 도구를 직접 붙인다.

<!--
[35분] 종료. 발표 순서는 시대순 STaR → R1 (강의 서사와 같은 방향).
-->
